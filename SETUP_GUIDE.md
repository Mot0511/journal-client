# 🎓 Электронный журнал - Полная инструкция по запуску

Система состоит из фронтенда (React + TypeScript) и бэкенда (Node.js + PostgreSQL).

## 📋 Что было создано

### ✅ Фронтенд (уже готов)
- React приложение с TypeScript
- Интерфейс для студентов и преподавателей
- Система оценок, посещаемости, лабораторных работ
- Настройка баллов и фильтрация

### ✅ Бэкенд (создан)
- Node.js API сервер с Express
- JWT аутентификация для студентов и преподавателей
- Подключение к PostgreSQL базе данных
- REST API для всех операций журнала
- Валидация данных и обработка ошибок
- Система безопасности с CORS и rate limiting

### ✅ База данных (используется существующая)
- PostgreSQL база данных `prpjectAS`
- Все необходимые таблицы уже созданы
- Связи между таблицами настроены

## 🚀 Запуск системы

### 1. Подготовка бэкенда

```bash
# Перейти в папку бэкенда
cd journal-main/backend

# Установить зависимости
npm install

# Проверить настройки в .env файле
# (файл уже создан с правильными настройками)
```

### 2. Запуск бэкенда

```bash
# Запуск в режиме разработки
npm run dev

# Или обычный запуск
npm start
```

Бэкенд будет доступен по адресу: `http://localhost:3000`

### 3. Запуск фронтенда

```bash
# В новом терминале, перейти в папку фронтенда
cd journal-main

# Установить зависимости (если не установлены)
npm install

# Запустить фронтенд
npm run dev
```

Фронтенд будет доступен по адресу: `http://localhost:5174`

## 🔧 Настройка базы данных

Убедитесь, что PostgreSQL запущен и база данных `prpjectAS` доступна с параметрами:
- **Хост**: localhost
- **Порт**: 5432
- **База**: prpjectAS
- **Пользователь**: postgres
- **Пароль**: sisisisisi123

## 🔐 Тестирование аутентификации

### Для тестирования API можно использовать:

1. **Postman** - импортировать запросы из файла `API_EXAMPLES.md`
2. **curl** - примеры команд в файле `backend/API_EXAMPLES.md`
3. **Фронтенд** - после интеграции с API

### Примеры тестовых запросов:

```bash
# Проверка здоровья сервера
curl http://localhost:3000/api/health

# Логин студента (замените на реальные данные из БД)
curl -X POST http://localhost:3000/api/auth/login/student \
  -H "Content-Type: application/json" \
  -d '{"login": "student_login", "password": "password"}'

# Логин преподавателя
curl -X POST http://localhost:3000/api/auth/login/teacher \
  -H "Content-Type: application/json" \
  -d '{"login": "teacher_login", "password": "password"}'
```

## 🔗 Интеграция фронтенда с бэкендом

### Для подключения фронтенда к API нужно:

1. **Создать API клиент** в фронтенде:

```javascript
// src/api/client.js
const API_BASE_URL = 'http://localhost:3000/api';

export const apiRequest = async (endpoint, options = {}) => {
  const token = localStorage.getItem('token');
  
  const config = {
    headers: {
      'Content-Type': 'application/json',
      ...(token && { Authorization: `Bearer ${token}` }),
      ...options.headers,
    },
    ...options,
  };

  const response = await fetch(`${API_BASE_URL}${endpoint}`, config);
  return response.json();
};
```

2. **Заменить моковые данные** в `src/data.ts` на реальные API вызовы

3. **Добавить систему авторизации** с формой логина

## 📁 Структура проекта

```
journal-main/
├── backend/                 # Node.js API сервер
│   ├── src/
│   │   ├── app.js          # Главный файл приложения
│   │   ├── config/         # Конфигурация БД
│   │   ├── controllers/    # Контроллеры API
│   │   ├── middleware/     # Middleware функции
│   │   ├── models/         # Модели данных
│   │   └── routes/         # Маршруты API
│   ├── .env               # Переменные окружения
│   ├── package.json       # Зависимости бэкенда
│   └── README.md          # Документация API
├── src/                   # React фронтенд
│   ├── components/        # React компоненты
│   ├── types/            # TypeScript типы
│   └── data.ts           # Данные (заменить на API)
├── package.json          # Зависимости фронтенда
└── README.md            # Документация проекта
```

## 🛠 API Endpoints

### Основные маршруты:

- **Аутентификация**: `/api/auth/*`
- **Студенты**: `/api/students/*`
- **Преподаватели**: `/api/teachers/*`
- **Группы**: `/api/groups/*`
- **Оценки**: `/api/grades/*`
- **Предметы**: `/api/subjects/*`

Полная документация в файле: `backend/README.md`

## 🔍 Отладка

### Логи сервера:
```bash
# В терминале бэкенда будут видны все запросы и ошибки
```

### Проверка подключения к БД:
```bash
# При запуске сервера должно появиться сообщение:
# "Connected to PostgreSQL database"
# "Database connection successful"
```

### Проверка API:
```bash
# Тест доступности API
curl http://localhost:3000/api/health
```

## ⚠️ Возможные проблемы

1. **Ошибка подключения к БД**: Проверьте, что PostgreSQL запущен и доступен
2. **Ошибка CORS**: Убедитесь, что фронтенд запущен на разрешенных портах
3. **Ошибка токена**: Проверьте, что JWT_SECRET установлен в .env файле

## 📚 Дополнительная информация

- **Документация API**: `backend/README.md`
- **Примеры запросов**: `backend/API_EXAMPLES.md`
- **Конфигурация БД**: `backend/src/config/database.js`

## ✨ Готово!

После выполнения всех шагов у вас будет работающая система электронного журнала с:
- ✅ Аутентификацией студентов и преподавателей
- ✅ Полнофункциональным API
- ✅ Интерфейсом для работы с оценками
- ✅ Безопасной работой с базой данных